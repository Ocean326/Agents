# Transfer_Recovery Project Skills

These skills originated from:

- `Transfer_Recovery/.agents/skills/`

They are project-local rather than fully general-purpose, so they live under
the `skills/project/transfer_recovery/` namespace instead of `skills/global/`.

Current skills:

- `transferrec-automation-governor`
- `transferrec-literature-chain`
