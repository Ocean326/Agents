interface:
  display_name: "Build Router"
  short_description: "构建与设计生产总入口，负责 UI、GitHub、部署与出图分流"
  default_prompt: "使用 $build-router 判断这个任务该走前端设计、GitHub 工作流、Vercel 部署，还是 imagegen / Figma / Canva 等精确工具。"
