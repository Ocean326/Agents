# Codex Config Examples

This folder is reserved for public-safe Codex configuration examples.

Planned example types:

- sanitized `config.toml` fragments
- example `AGENTS.md` patterns
- example automation prompt snippets
- machine-agnostic workflow notes

Do not commit:

- real secrets
- auth tokens
- machine-specific private paths unless they are clearly examples
